info:
  name: All For User
  type: http
  seq: 2

http:
  method: GET
  url: "{{SERVER_URL}}/api/v1/observation-sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
