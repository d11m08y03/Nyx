info:
  name: Update Session
  type: http
  seq: 4

http:
  method: PUT
  url: "{{SERVER_URL}}/api/v1/observation-sessions/{{SESSION_ID}}"
  body:
    type: json
    data: |-
      {
        "filter_id": "43ca8b6a-e24d-410a-a075-8df4e576e158",
        "notes": "Updated observation notes"
      }
  auth: inherit

runtime:
  variables:
    - name: SESSION_ID
      value: 04f7572a-16be-4b4c-8e76-3c522605078e

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
