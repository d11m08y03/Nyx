info:
  name: Invalid Request
  type: http
  seq: 1

http:
  method: POST
  url: "{{SERVER_URL}}/api/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "123e4",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
