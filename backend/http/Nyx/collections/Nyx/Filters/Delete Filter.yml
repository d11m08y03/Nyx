info:
  name: Delete Filter
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{SERVER_URL}}/api/v1/filters/{{FILTER_ID}}"
  auth: inherit

runtime:
  variables:
    - name: FILTER_ID
      value: af37517e-1a00-4534-a9e7-8f76203b28bf

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
