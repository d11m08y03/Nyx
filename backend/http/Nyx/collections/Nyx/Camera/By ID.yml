info:
  name: By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{SERVER_URL}}/api/v1/cameras/{{CAMERA_ID}}"
  auth: inherit

runtime:
  variables:
    - name: CAMERA_ID
      value: 6a65d7cc-4da8-4a2c-a249-a1633741dafa

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
