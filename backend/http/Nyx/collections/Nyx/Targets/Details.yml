info:
  name: Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{SERVER_URL}}/api/v1/targets/{{TARGET_ID}}"
  auth: inherit

runtime:
  variables:
    - name: TARGET_ID
      value: 5a576cfa-cfe7-42b8-b1a3-46fa6e720a04

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
