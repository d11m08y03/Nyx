info:
  name: Fetch Light Curve
  type: http
  seq: 5

http:
  method: POST
  url: "{{SERVER_URL}}/api/v1/tess-observations/{{TESS_OBSERVATION_ID}}/fetch-light-curve"
  auth: inherit

runtime:
  variables:
    - name: TESS_OBSERVATION_ID
      value: aed1f798-0dc9-4aba-a5ec-02c00cdafea5

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
