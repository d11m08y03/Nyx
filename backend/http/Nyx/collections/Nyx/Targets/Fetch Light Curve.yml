info:
  name: Fetch Light Curve
  type: http
  seq: 5

http:
  method: POST
  url: "{{SERVER_URL}}/api/v1/tess-observations/{{TESS_OBSERVATION_ID}}/fetch-light-curve"
  auth: inherit

runtime:
  variables:
    - name: TESS_OBSERVATION_ID
      value: 4f8acaa8-9c51-42f4-92e0-39b8744ebca1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
