info:
  name: TESS Observations
  type: http
  seq: 3

http:
  method: GET
  url: "{{SERVER_URL}}/api/v1/targets/{{TARGET_ID}}/tess-observations"
  auth: inherit

runtime:
  variables:
    - name: TARGET_ID
      value: 384d4d13-eaf9-4a85-ae19-df72707bb685

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
