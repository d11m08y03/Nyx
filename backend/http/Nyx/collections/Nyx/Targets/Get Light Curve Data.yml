info:
  name: Get Light Curve Data
  type: http
  seq: 6

http:
  method: GET
  url: "{{SERVER_URL}}/api/v1/tess-observations/{{TESS_OBSERVATION_ID}}/light-curve?quality_filter=true"
  params:
    - name: quality_filter
      value: "true"
      type: query
  auth: inherit

runtime:
  variables:
    - name: TESS_OBSERVATION_ID
      value: aed1f798-0dc9-4aba-a5ec-02c00cdafea5

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
